import { ReservationHighlightDirective } from './reservation-highlight.directive';

describe('ReservationHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new ReservationHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
